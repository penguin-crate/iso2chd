# iso2chd

An interactive batch ISO to CHD conversion utility for Arch Linux by **Abhimanyu Bhadauriya**.

## Installation

### Using 'git' (requires packages 'git' and 'base-devel' installed)
```bash
git clone https://github.com/penguin-crate/iso2chd/iso2chd.git
cd iso2chd
makepkg -si
